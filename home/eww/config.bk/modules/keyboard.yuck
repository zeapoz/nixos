(deflisten keyboard_lang "scripts/keyboard")
(defwidget keyboard []
  (box :class "module keyboard"
       :orientation "v"
       :space-evenly false
    (label :class "icon" :text "")
    (label :text "${keyboard_lang}")))
