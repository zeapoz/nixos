(deflisten net "scripts/net")
(defwidget net []
  (box :class "net"
    (button
      :onclick "scripts/net toggle"
      "${net.icon} ")
    (button
      :onclick "mullvad-gui &"
      "${net.vpn}")))
