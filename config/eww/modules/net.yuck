(deflisten net "scripts/net")
(defwidget net []
  (box :class "module net"
       :orientation "v"
       :space-evenly false
    (button :class "button icon" :onclick "scripts/net toggle" "${net.icon}")
    (button :class "button icon" :onclick "mullvad-gui &" "${net.vpn}")))
